export const endpoint = `http://localhost:3000/api/graphql`;
export const prodEndpoint = `a.future.url`;
export const perPage = 4;

// DAVE ask about merging it branches and needing to reinstall npm packages in order to run program
